import { ProfileLine } from '../components/profile-line/index';

type ProfileProps = {
  email?: ProfileLine;
  login?: ProfileLine;
  first_name?: ProfileLine;
  second_name?: ProfileLine;
  display_name?: ProfileLine;
  phone?: ProfileLine;
}

export {
    ProfileProps
};
